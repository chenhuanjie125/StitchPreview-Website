# StitchPreview website

Static, dependency-free website source. Preview locally with:

```sh
cd web
python3 -m http.server 8080
```

Then open `http://localhost:8080/`. Do not publish this folder until local acceptance is recorded in `PROJECT_NOTES.md`.
